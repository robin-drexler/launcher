  (function($){
    $.fn.launcher = function(options) {
      return this.each(function() {
	    var $this = $(this);
        /*options = $.extend({
            'maxLength': 100,
			'align': 'right',
			'valign': 'bottom',
			'show': 'auto',
			'elCount': 'auto'
		}, options);*/
        alert('ddd');
        
        
        function blu() {
          alert('blub');  
        };
				
	  });
    };

   

	
  })(jQuery);
  
  
  
    